spring:
  application:
    name: backend
  profiles:
    active: dev
  mvc:
    servlet:
      path: /api/v1


app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
